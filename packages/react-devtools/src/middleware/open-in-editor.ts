/**
 * Open in editor middleware.
 *
 * Delegates to react-tracer's middleware module and re-exports for
 * backward compatibility.
 */

export {
  createOpenInEditorMiddleware,
  openFileInEditor,
} from 'react-tracer/middleware'
